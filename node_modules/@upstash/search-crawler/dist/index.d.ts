interface CrawlerOptions {
  upstashUrl: string;
  upstashToken: string;
  indexName?: string;
  docUrl: string;
}

interface CrawlerResult {
  success: boolean;
  newRecordsCount: number;
  totalRecordsCount: number;
  error?: string;
}

declare function crawlAndIndex(options: CrawlerOptions): Promise<CrawlerResult>;

export { type CrawlerOptions, type CrawlerResult, crawlAndIndex };
